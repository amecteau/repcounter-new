<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1024 1024">
  <!-- Background -->
  <rect width="1024" height="1024" rx="180" fill="#1e293b"/>
 
  <!-- Bar -->
  <rect x="311" y="512" width="401" height="46" rx="12" fill="#94a3b8"/>
 
  <!-- Left large plate (inner, closest to bar) -->
  <rect x="211" y="350" width="100" height="370" rx="20" fill="#f1f5f9"/>
  <!-- Left small plate (outer) -->
  <rect x="134" y="419" width="77"  height="231" rx="14" fill="#cbd5e1"/>
  <!-- Left end cap -->
  <rect x="80"  y="466" width="54"  height="139" rx="10" fill="#e2e8f0"/>
 
  <!-- Right large plate (inner, closest to bar) -->
  <rect x="712" y="350" width="100" height="370" rx="20" fill="#f1f5f9"/>
  <!-- Right small plate (outer) -->
  <rect x="812" y="419" width="77"  height="231" rx="14" fill="#cbd5e1"/>
  <!-- Right end cap -->
  <rect x="889" y="466" width="54"  height="139" rx="10" fill="#e2e8f0"/>
</svg>
